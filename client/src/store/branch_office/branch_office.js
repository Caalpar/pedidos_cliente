import { writable } from 'svelte/store';
export const branch_officeData = writable({});
export const branch_officeAllData = writable([]);
export const currentIdBranchOfficeId = writable('');
export const currentDataBranchOffice = writable({});

