import { writable } from 'svelte/store';
export const businessData = writable({});
export const currentIdBusiness = writable('');
export const currentDataBusiness = writable({});
export const businessAllData = writable([]);
